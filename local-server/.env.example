# ── Node Exporter Endpoints ───────────────────────────────────────────────────
MINIPC_NODE_EXPORTER=192.168.0.43:9100
LLM_NODE_EXPORTER=192.168.0.73:9100

# ── GPU Exporter ──────────────────────────────────────────────────────────────
LLM_GPU_EXPORTER=192.168.0.73:9835

# ── API-Token (Schutz bei Tailscale Funnel) ───────────────────────────────────
# Gleiches Token in All-Inkl .env als MINIPC_TOKEN eintragen
# Leer lassen = kein Token-Check (nur für rein lokale Nutzung)
LOCAL_API_TOKEN=
